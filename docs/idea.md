# Lua-native Neovim Coding Agent

Build a Neovim plugin that implements a full coding agent experience natively in Lua — chat interface, file operations, tool execution, session management, diff review — without relying on an external `pi` subprocess. All LLM interaction, tool dispatching, and UI rendering happens inside Neovim via Lua.

